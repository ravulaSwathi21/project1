pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
              echo "building the job"
            }
        }
        stage('Test') { 
            steps {
             echo "testing the job"
            }
        }
        stage('Deploy') { 
            steps {
              echo "deploying  the job"
            }
        }
    }
}

